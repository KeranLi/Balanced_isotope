# Local Data

This directory contains local research inputs and small example fixtures.

Research datasets are intentionally ignored by Git and remain on the local
machine only. The tracked example files are used by demonstrations and tests.

Private inputs currently used locally include the Nie Mg profiles, Sr tables,
and other study-specific spreadsheets. Do not add them with `git add -f` unless
their redistribution status has been reviewed.
